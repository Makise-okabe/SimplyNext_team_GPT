GROQ_API_KEY=

# Microsoft Graph delegated auth for Outlook
# Put these values in your local .env file. Never commit .env.
MS_CLIENT_ID=
MS_TENANT_ID=organizations

# Track A auth experiment:
# interactive = system-browser OAuth using http://localhost callback
# device      = previous device-code flow for comparison/debugging
MS_AUTH_MODE=interactive
